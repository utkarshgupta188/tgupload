# Backend configuration
API_PASSWORD=changeme-strong-password

# Mode: bot (<=50MB) or user (<=2GB)
TELEGRAM_UPLOAD_MODE=user

# Bot mode (if using bot)
TELEGRAM_BOT_TOKEN=
TG_CHAT_ID=

# User mode (MTProto via Pyrogram)
TG_API_ID=
TG_API_HASH=
TG_SESSION_STRING=
# Target chat (user mode uploads will be sent here)
TG_CHAT_ID=

# Database (optional; default is SQLite in backend/data/app.db)
# DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@HOST:PORT/DBNAME

# CORS base (optional)
# BASE_URL=https://your-frontend.vercel.app
